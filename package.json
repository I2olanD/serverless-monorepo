{
  "name": "serverless-monorepo",
  "private": true,
  "devDependencies": {
    "lerna": "^3.20.2"
  },
  "scripts": {
    "invoke": "lerna run invoke --no-bail --no-prefix",
    "build": "lerna run build --no-bail --stream --no-prefix",
    "test": "lerna run test --no-bail --no-prefix"
  },
  "engines": {
    "node": "12.x"
  }
}
